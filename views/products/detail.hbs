<div class="container">

    <h2 class="c-dark-gray font-weight-semi mb-4">Detalle del producto</h2>

    <div class="row mx-auto bg-white mx-auto mb-4">
        <div class="col-md-4 col-sm-12 py-2">
            <img src="{{product.main_image}}" class="w-100 rounded border" />
        </div>

        <div class="col-md-6 col-sm-12 py-2">
            <h3 class="font-weight-semi">{{product.title}}</h3>

            <p>{{{product.fashion_main_description}}}</p>
        </div>
        <div class="col-md-6 col-sm-12 py-2">
            <h3 class="font-weight-semi">{{product.title}}</h3>

            <p>{{{product.fashion_main_description}}}</p>
        </div>

        <div>
            {{#each variants as |variant|}}
                <a href="addtocart">{{variant.sizes}}</a>
            {{/each}}
        </div>

    </div>
</div>