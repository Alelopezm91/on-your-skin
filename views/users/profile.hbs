<div class="container">
    <div>
    <h1>{{currentUser.name}}</h1>
    <img src="{{currentUser.image}}" class="profile">
    </div>
        {{#each likes as |like|}}
<div class="col-xl-3 col-sm-6 col-xs-12 gy-4 d-flex align-items-stretch">
    <div class="card w-100 " style="width: 18rem;">
        <div class="position-relative">
            <img src="{{like.product.main_image}}" class="card-img-top" alt="...">
        </div>
        <div class="card-body d-flex flex-column mt-2 justify-content-between">
            <div>
                <h6 class="card-title py-1">{{like.product.title}}</h6>
                <small class="card-text text-muted text-small">{{like.product.fashion_product_type}}</small>
                <h5 class="card-title mb-4 mt-2">{{like.product.RSVP}} €</h5>
            </div>
            <a href="/products/detail/{{like.product._id}}" class="btn btn btn-outline-primary">Detalle</a>
        </div>
    </div>
</div>
        {{/each}}
</div>